PORT=3000
SUPABASE_URL=
# Service-role (secret) key from Supabase → Project Settings → API → service_role
# Used for JWT verification and RLS-scoped queries. Never expose in client code.
SUPABASE_SECRET_KEY=
